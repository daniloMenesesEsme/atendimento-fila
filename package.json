{
  "name": "atendimento-fila",
  "version": "1.0.0",
  "description": "Sistema de Atendimento de Fila",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 